"""Versión de la app.

Fuente única: se muestra en una esquina de todas las pantallas (vía
`/api/version` + `static/version.js`) y en la doc de la API. Para publicar una
versión nueva, cambiá solo este número.
"""

VERSION = "26.3"
